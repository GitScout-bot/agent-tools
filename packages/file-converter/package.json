{
  "name": "@agent-tools/file-converter",
  "version": "0.1.0",
  "description": "Convert between common file formats (JSON, YAML, TOML, CSV)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test src/__tests__/converter.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["converter", "json", "yaml", "csv", "toml", "agent", "ai"],
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
